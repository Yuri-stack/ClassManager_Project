{% extends "layout.njk" %}

{% block content %}

    <div class="card table-container">
        
        <table width=100%>
            <thead>
                <tr>
                    <th>Nome Completo</th>
                    <th>Acompanhamento</th>
                    <th>Ação</th>
                </tr>
            </thead>

            <tbody>
                {% for teacher in teachers %}

                <tr>
                    <td>
                        <span style = "background-image: url({{ teacher.avatar_url }})"></span>
                        {{ teacher.name }}
                    </td>
                    <td>
                        {{ teacher.acting }}
                    </td>
                    <td>
                    {# <span><a href="/teachers/{{ teacher.id }}">Ver</a></span>
                    <span><a href="/teachers/{{ teacher.id }}">Ver</a></span> #}

                    {# <div>
                        <a href="/teachers/{{ teacher.id }}">Ver</a>
                    </div>
                    <div>
                        <a href="/teachers/{{ teacher.id }}">Ver</a>
                    </div> #}

                    <a href="/teachers/{{ teacher.id }}">Ver</a>

                    <a href="/teachers/{{ teacher.id }}/edit">Editar</a>
                        
                    </td>
                </tr>

                {% endfor %}
            </tbody>

        </table>

    </div>

{% endblock content %}